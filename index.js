
Domselectors = {
    //store all related code for selecting documents here
    form: document.querySelector("#form"),
    firstName: document.querySelector("#name"),
    h2s: document.querySelector("h2")
};

Domselectors.form.addEventListener("submit", function(event){
    event.preventDefault();
Domselectors.h2s.array.forEach(header => {header.textContext = Domselectors.firstName.value
    
});
});



